[project]
name = "blaze"
version = "0.1.3"

[tasks]
serve.script = "fleet run --release"
publish.script = [
	"cargo publish --package blaze --no-verify", 
	"cargo publish --package blaze-macros --no-verify"
]